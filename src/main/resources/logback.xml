<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <property name="log.path" value="./logs"></property>
    <property name="log.context" value="cabs-cache"></property>
    <contextName>${log.context}</contextName>

    <!--控制台输出-->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>[%d{yyyy-MM-dd HH:mm:ss.SSS}][${log.context}][%-5level][%thread][%X{showTex}][%class.%method:%line]:%m%n</pattern>
        </encoder>
    </appender>

    <!--每天打印一个log, yyyy/MM/dd 树形目录-->
    <appender name="ALL" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!--<fileNamePattern>${SYS_LOG_DIR}/${HOSTNAME}${LOG_FILE}%d{yyyy-MM-dd}.log</fileNamePattern>
            日志文件保留天数<maxhistory>30</maxhistory>-->
            <fileNamePattern>${log.path}/${log.context}/${HOSTNAME}-%d{yyyyMMdd}.log</fileNamePattern>
        </rollingPolicy>
        <encoder>
            <pattern>[%d{yyyy-MM-dd HH:mm:ss.SSS}][${log.context}][%-5level][%thread][%class{1}.%method:%line][%X{trcId}][%X{ctxTag}]%m%n
            </pattern>
        </encoder>
        <!--<append>true</append>
        <prudent>true</prudent>-->
    </appender>

    <!-- 文件 异步日志(async) -->
    <appender name="ASYNC-STDOUT" class="ch.qos.logback.classic.AsyncAppender">
        <!-- 不丢失日志.默认的,如果队列的80%已满,则会丢弃TRACT、DEBUG、INFO级别的日志 -->
        <discardingThreshold>0</discardingThreshold>
        <!-- 更改默认的队列的深度,该值会影响性能.默认值为256 -->
        <queueSize>2048</queueSize>
        <neverBlock>true</neverBlock>
        <includeCallerData>true</includeCallerData>
        <!-- 添加附加的appender,最多只能添加一个 -->
        <appender-ref ref="ALL"/>
    </appender>

    <!-- 文件 异步日志(async) -->
    <appender name="ASYNC-ALL" class="ch.qos.logback.classic.AsyncAppender">
        <!-- 不丢失日志.默认的,如果队列的80%已满,则会丢弃TRACT、DEBUG、INFO级别的日志 -->
        <discardingThreshold>0</discardingThreshold>
        <!-- 更改默认的队列的深度,该值会影响性能.默认值为256 -->
        <queueSize>2048</queueSize>
        <neverBlock>true</neverBlock>
        <includeCallerData>true</includeCallerData>
        <!-- 添加附加的appender,最多只能添加一个 -->
        <appender-ref ref="STDOUT"/>
    </appender>

    <!--打印任意包日志-->
    <root level="INFO">
        <appender-ref ref="ASYNC-STDOUT"/>
        <appender-ref ref="ASYNC-ALL"/>
    </root>
</configuration>